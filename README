# Documentation de l'API UberEats

Bienvenue dans la documentation de l'API UberEats. Cette API est construite avec Node.js, Express et Swagger pour la documentation.

## Installation

1. Assurez-vous d'avoir Node.js installé sur votre machine.
2. Clonez ce dépôt sur votre machine.
3. Exécutez `npm install` pour installer les dépendances.

## Démarrage du serveur

Une fois les dépendances installées, vous pouvez démarrer le serveur en exécutant la commande suivante :

npm start

## Mon Seveur
Le serveur sera alors accessible à l'adresse http://localhost:3000.

Endpoints disponibles
/auth (POST) : Endpoint pour l'authentification des utilisateurs.
/user/{id} (GET) : Récupère un utilisateur par son ID.
// Ajoutez d'autres endpoints disponibles avec leur description ici

Documentation Swagger
Vous pouvez consulter la documentation Swagger de l'API en accédant à l'URL suivante :

http://localhost:3000/api-docs